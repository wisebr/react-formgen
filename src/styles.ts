import { makeStyles } from '@material-ui/styles';

export const useCommonStyles = makeStyles({
  settingField: {
    display: 'block',
    marginBottom: 10,
  },
  element: {
    marginRight: 10,
  },
}, {name: 'fg-common'});