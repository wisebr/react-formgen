import { makeStyles } from '@mui/styles';

export const useCommonStyles = makeStyles({
  settingField: {
    display: 'block',
    marginBottom: 15,
  },
  element: {
    marginRight: 10,
  },
}, {name: 'fg-common'});